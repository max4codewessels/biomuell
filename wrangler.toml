name = "runterbringen-tracker"
main = "main.js"
compatibility_date = "2024-01-01"
compatibility_flags = ["nodejs_compat"]

# KV Namespace für die Speicherung der Daten
[[kv_namespaces]]
binding = "RUNTERBRINGEN_KV"
id = "xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"  # ID aus dem ersten Befehl
preview_id = "yyyyyyyyyyyyyyyyyyyyyyyyyyyyyyyy"  # ID aus --preview Befehl

[env.production]
vars = { ENVIRONMENT = "production" }

[vars]
SITE_TITLE = "Runterbringen Tracker"
ADMIN_USERS = "max,maya"

[env.staging]
name = "runterbringen-tracker-staging"
vars = { ENVIRONMENT = "staging" }

logpush = false